# Task Brief: C1

**Title:** Create shared ImpactPoint type
**PRD:** data-model-unify
**Priority:** must
**Complexity:** 3/10
**Model:** sonnet
**Wave:** 1

---

## Objective

Create `types/shared.ts` — a new file exporting the unified `ImpactPoint` interface. This single type replaces three parallel impact-metric shapes currently spread across `types/narrative.ts`, `types/learning-site.ts`, and `types/project.ts`. This is the foundation task; nothing else in the PRD can proceed until this file exists.

---

## Context

**Parent Feature:** data-model-unify (PRD-C schema cleanup)

The Barbets Duet codebase currently has three separate interfaces representing the same concept — an impact metric displayed in story, project, and learning-site contexts:

- `ImpactMetric` in `types/narrative.ts` — `{ label, value, unit }` (unit required)
- `ImpactStat` in `types/learning-site.ts` — `{ label, value, description, trend? }` (no unit, has description)
- `ProjectImpact` in `types/project.ts` — `{ label, value, unit? }` (unit optional)

These are structurally close but not identical. A unified shape that is the **superset** of all three allows all three to be replaced with type aliases pointing here, without breaking any existing data.

This task is part of **Wave 1 — Foundation**. C2 depends on this file existing before it can run.

---

## Requirements

1. Create the file `types/shared.ts` (it does not exist yet — `ls types/` confirms: narrative.ts, project.ts, team.ts, community.ts, learning-site.ts, forum.ts).
2. Export a single interface `ImpactPoint` with these exact fields:
   - `label: string` — human-readable metric name
   - `value: string` — the metric number/value as a formatted string
   - `unit?: string` — optional unit suffix (e.g. "ha", "%", "tonnes CO₂")
   - `description?: string` — optional longer explanation (used by LearningSite impactData)
   - `trend?: 'up' | 'down'` — optional trend indicator (used by LearningSite impactData)
3. Include a JSDoc block explaining the unification rationale.
4. No other exports in this file at this stage.

---

## Success Criteria

- [ ] `types/shared.ts` exists at the project root types directory
- [ ] `ImpactPoint` is exported from `types/shared.ts`
- [ ] All five fields are present with the exact types specified above
- [ ] `unit`, `description`, and `trend` are optional (marked with `?`)
- [ ] `trend` is typed as the literal union `'up' | 'down'`, not `string`
- [ ] The file includes a JSDoc comment block explaining why this type exists and what it replaces
- [ ] `npx tsc --noEmit` passes (file is syntactically valid TypeScript)

---

## Files to Modify

| File | Action | Purpose |
|------|--------|---------|
| `types/shared.ts` | create | New file — exports `ImpactPoint` interface |

---

## Implementation Guidance

### Exact file content to produce

```typescript
/**
 * Shared impact metric shape used across Story, Project, and LearningSite contexts.
 *
 * Replaces three parallel interfaces that existed before PRD-C unification:
 *   - ImpactMetric (types/narrative.ts)   — { label, value, unit }
 *   - ProjectImpact (types/project.ts)    — { label, value, unit? }
 *   - ImpactStat (types/learning-site.ts) — { label, value, description, trend? }
 *
 * ImpactPoint is a strict superset of all three — no existing data is broken.
 * `description` and `trend` are only populated by LearningSite impactData.
 * `unit` is only populated by Story and Project impact metrics.
 */
export interface ImpactPoint {
  label: string;
  value: string;
  unit?: string;
  description?: string;
  trend?: 'up' | 'down';
}
```

### Patterns to Follow

- All other files in `types/` use named exports, no default exports. Follow the same pattern.
- TypeScript strict mode is enforced project-wide (`tsconfig.json` — do not change it).
- No `any` types, no `unknown` without narrowing.

### Edge Cases

- Do not add barrel exports or re-exports in this file yet — C2 handles the migration.
- Do not modify any existing type files in this task — that is C2's scope.
- Do not add `index.ts` to `types/` — the project imports types directly by path.

---

## Dependencies

### Upstream Tasks

None — C1 has no dependencies. This is the first task in Wave 1.

### Downstream Impact

Tasks that depend on this one: **C2** (migrates all three source type files to use ImpactPoint).

---

## Boundaries

### Files You MUST NOT Touch

- `types/narrative.ts` — that is C2's scope
- `types/learning-site.ts` — that is C2's scope
- `types/project.ts` — that is C2's scope
- Any component files — those are C3's scope
- `package.json` — no new dependencies needed for this task

---

## Validation Checklist

Before marking task done:
- [ ] `types/shared.ts` exists
- [ ] `npx tsc --noEmit` exits 0
- [ ] No modifications to any file other than `types/shared.ts`

---

*Generated by KARIMO Brief Writer*
*PRD: data-model-unify | Task: C1 | Wave: 1*
