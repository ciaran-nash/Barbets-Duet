# Task Brief: C3

**Title:** Update ImpactGrid, ProjectCard, ProjectDetailDrawer imports
**PRD:** data-model-unify
**Priority:** must
**Complexity:** 1/10
**Model:** sonnet
**Wave:** 2

---

## Objective

Verify that the three components that consume impact-metric types compile cleanly after C2's migration. Because C2 keeps the old type names as aliases (`ImpactStat`, `ProjectImpact`), existing imports in these components will continue to resolve. This task verifies that is true, and updates any import that references the type directly (rather than via the alias) to use `ImpactPoint` explicitly if appropriate.

---

## Context

**Parent Feature:** data-model-unify (PRD-C schema cleanup)

After C2 completes (Wave 1), the type aliases mean existing component imports do not need to change at the type-import level. However, three components use these types directly in their prop or local function signatures:

**`components/learning-sites/ImpactGrid.tsx`** (line 5):
```typescript
import { LearningSite, ImpactStat } from '@/types/learning-site';
```
The `StatItem` sub-component on line 63 has `{ stat }: { stat: ImpactStat }`.

**`components/projects/ProjectCard.tsx`** (line 6):
```typescript
import { Project } from '@/types/project';
```
Uses `project.impactMetrics` on line 64 — accesses `.label`, `.value`, `.unit`. No explicit `ProjectImpact` import.

**`components/projects/ProjectDetailDrawer.tsx`** (line 6):
```typescript
import { Project } from '@/types/project';
```
Uses `project.impactMetrics` on lines 91-94 — accesses `.label`, `.value`, `.unit`. No explicit `ProjectImpact` import.

This task is part of **Wave 2**. C3 runs in parallel with C4 and C6 (they are all independent of each other).

---

## Requirements

1. Open each of the three component files and run `npx tsc --noEmit` — verify zero errors related to these files.
2. If any component imports the old type name **as a named import** (e.g. `import { ImpactStat }`), this will still work via the alias, but optionally update it to `import { ImpactPoint } from '@/types/shared'` for clarity. This is optional but preferred.
3. No logic changes. No visual changes. No new props.
4. Confirm `ImpactGrid.tsx`'s `StatItem` function still renders `stat.description` and `stat.trend` correctly — these fields now come from `ImpactPoint` (same shape, now optional rather than required).

### Specific check for ImpactGrid

The `StatItem` function currently renders:
```tsx
{stat.trend === 'up' && (
  <ArrowUpRight className="w-8 h-8 text-brand animate-pulse" />
)}
...
<p className="...">
  {stat.description}
</p>
```

With `ImpactPoint`, `description` is `string | undefined`. If TypeScript strict mode flags `stat.description` as potentially undefined in JSX, add a fallback: `{stat.description ?? ''}`. Check whether TypeScript raises this error — if it does, fix it; if it does not (JSX renders undefined as nothing), leave it.

---

## Success Criteria

- [ ] `components/learning-sites/ImpactGrid.tsx` compiles without TypeScript errors
- [ ] `components/projects/ProjectCard.tsx` compiles without TypeScript errors
- [ ] `components/projects/ProjectDetailDrawer.tsx` compiles without TypeScript errors
- [ ] No runtime behaviour changes — impact metrics display identically to before
- [ ] `npx tsc --noEmit` exits 0 across the project

---

## Files to Modify

| File | Action | Purpose |
|------|--------|---------|
| `components/learning-sites/ImpactGrid.tsx` | modify (likely minor) | Update ImpactStat import if needed; fix any strict-null issue with description |
| `components/projects/ProjectCard.tsx` | verify / modify | Confirm no errors; no logic changes expected |
| `components/projects/ProjectDetailDrawer.tsx` | verify / modify | Confirm no errors; no logic changes expected |

### Files to Read for Context

| File | Why |
|------|-----|
| `types/shared.ts` | Confirm ImpactPoint shape (created by C1) |
| `types/learning-site.ts` | Confirm ImpactStat alias (set by C2) |
| `types/project.ts` | Confirm ProjectImpact alias (set by C2) |

---

## Implementation Guidance

### Current ImpactGrid import (from codebase scan)

```typescript
import { LearningSite, ImpactStat } from '@/types/learning-site';
```

After C2, this import continues to resolve because `ImpactStat` is now an exported type alias. No change strictly required. However, if you update it for clarity:

```typescript
import { LearningSite } from '@/types/learning-site';
import type { ImpactPoint } from '@/types/shared';
```

And change the `StatItem` signature from `{ stat: ImpactStat }` to `{ stat: ImpactPoint }`.

### ProjectCard and ProjectDetailDrawer

Neither file imports `ProjectImpact` directly — both work through `Project.impactMetrics` which is typed as `ImpactPoint[]` after C2. No import changes are needed. Just verify they compile.

### Strict-null handling for description

`ImpactPoint.description` is `string | undefined`. In the `StatItem` render:
```tsx
<p className="...">{stat.description}</p>
```
React renders `undefined` as nothing (no visible difference), and TypeScript does not error on `{undefined}` in JSX. So no change is needed unless tsc raises an error, which it should not.

---

## Dependencies

### Upstream Tasks

| Task | What It Provides | Verify Before Starting |
|------|------------------|------------------------|
| C2 | ImpactStat, ProjectImpact aliases in type files | Run `npx tsc --noEmit` — must pass before starting |

### Downstream Impact

Tasks that depend on this one: **C7** (final typecheck — depends on C3, C5, C6 all completing).

---

## Boundaries

### Files You MUST NOT Touch

- `types/shared.ts` — do not modify
- `types/narrative.ts`, `types/learning-site.ts`, `types/project.ts` — C2 is done; do not re-open
- `components/stories/CinematicReader.tsx` — C5's scope
- `lib/sanity/queries.ts` — C6's scope

---

## Validation Checklist

Before marking task done:
- [ ] All success criteria met
- [ ] `npx tsc --noEmit` exits 0
- [ ] Only the three component files modified (or confirmed no-op)
- [ ] No logic changes introduced

---

*Generated by KARIMO Brief Writer*
*PRD: data-model-unify | Task: C3 | Wave: 2*
